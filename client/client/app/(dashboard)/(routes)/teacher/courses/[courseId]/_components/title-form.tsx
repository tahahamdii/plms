'use client'

interface TitleFormProps {
    initialData: {
        title: string;

    };
    courseId: string;
};

export const TitleForm = ({
    initialData,
    courseId
}:TitleFormProps) => {
    return (
        <div>
        title
        </div>
    )
}